#!/bin/sh

set -ue

TARGET=$1

( cd $TARGET ; docker build -t rust_$TARGET . )
mkdir -p build/$TARGET
docker run -it --rm -v $(pwd)/../:/root/src/:z -v $(pwd)/build/$TARGET:/root/src/target:z -w /root/src rust_$TARGET cargo rpm build
